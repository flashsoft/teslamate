diff --git a/.github/actions/build/action.yml b/.github/actions/build/action.yml
index 9a03b231..6e8f32bd 100644
--- a/.github/actions/build/action.yml
+++ b/.github/actions/build/action.yml
@@ -8,6 +8,9 @@ inputs:
   docker_password:
     description: "Docker password"
     required: true
+  docker_username:
+    description: "Docker Hub username"
+    required: true
   repository_owner:
     description: "Repository owner"
     required: true
@@ -39,6 +42,13 @@ runs:
         NORMALIZED_VERSION="${VERSION//\//-}"
         echo "normalized_version=${NORMALIZED_VERSION}" >> $GITHUB_OUTPUT
 
+    - name: normalize repository name for registry refs
+      id: normalize_repository
+      shell: bash
+      run: |
+        REPOSITORY="${{ inputs.repository }}"
+        echo "normalized_repository=${REPOSITORY,,}" >> $GITHUB_OUTPUT
+
     - name: Docker meta
       id: meta
       uses: docker/metadata-action@030e881283bb7a6894de51c315a6bfe6a94e05cf # v6.0.0
@@ -52,7 +62,7 @@ runs:
       if: inputs.is_dockerhub_pushed != ''
       uses: docker/login-action@b45d80f862d83dbcd57f89517bcf500b2ab88fb2 # v4.0.0
       with:
-        username: teslamate
+        username: ${{ inputs.docker_username }}
         password: ${{ inputs.docker_password }}
     - name: Login to GitHub Container Registry
       uses: docker/login-action@b45d80f862d83dbcd57f89517bcf500b2ab88fb2 # v4.0.0
@@ -69,9 +79,9 @@ runs:
         labels: ${{ steps.meta.outputs.labels }}
         tags: ${{ steps.docker_meta.outputs.tags }}
         cache-from: |
-          type=registry,ref=ghcr.io/${{ inputs.repository }}:buildcache-${{ matrix.cache_id }}-${{ steps.normalize_version.outputs.normalized_version }}
-          type=registry,ref=ghcr.io/${{ inputs.repository }}:buildcache-${{ matrix.cache_id }}-main
-        cache-to: type=registry,ref=ghcr.io/${{ inputs.repository }}:buildcache-${{ matrix.cache_id }}-${{ steps.normalize_version.outputs.normalized_version }},mode=max
+          type=registry,ref=ghcr.io/${{ steps.normalize_repository.outputs.normalized_repository }}:buildcache-${{ matrix.cache_id }}-${{ steps.normalize_version.outputs.normalized_version }}
+          type=registry,ref=ghcr.io/${{ steps.normalize_repository.outputs.normalized_repository }}:buildcache-${{ matrix.cache_id }}-main
+        cache-to: type=registry,ref=ghcr.io/${{ steps.normalize_repository.outputs.normalized_repository }}:buildcache-${{ matrix.cache_id }}-${{ steps.normalize_version.outputs.normalized_version }},mode=max
         outputs: type=image,name=${{ env.REGISTRY_IMAGE }},push-by-digest=true,name-canonical=true,push=true
     - name: Export digest
       shell: bash
diff --git a/.github/workflows/buildx.yml b/.github/workflows/buildx.yml
index cb95bc64..de248086 100644
--- a/.github/workflows/buildx.yml
+++ b/.github/workflows/buildx.yml
@@ -14,7 +14,7 @@ on:
       - "!.github/workflows/**" # Important: Exclude PRs related to .github/workflows from auto-run
       - "!.github/actions/**" # Important: Exclude PRs related to .github/actions from auto-run
 env:
-  REGISTRY_IMAGE: teslamate/teslamate
+  REGISTRY_IMAGE: babyworld/teslamate
 
 permissions:
   contents: read
@@ -26,7 +26,7 @@ jobs:
 
   teslamate_build:
     needs: check_paths
-    if: needs.check_paths.outputs.githubfolder == 'false' || github.event_name == 'schedule'
+    if: needs.check_paths.outputs.githubfolder == 'false' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
     strategy:
       fail-fast: false
       matrix:
@@ -51,6 +51,7 @@ jobs:
         uses: ./.github/actions/build
         with:
           is_dockerhub_pushed: "1"
+          docker_username: ${{ secrets.DOCKER_USERNAME }}
           docker_password: ${{ secrets.DOCKER_PASSWORD }}
           repository_owner: ${{ github.repository_owner }}
           repository: ${{ github.repository }}
@@ -62,7 +63,7 @@ jobs:
     needs:
       - check_paths
       - teslamate_build
-    if: needs.check_paths.outputs.githubfolder == 'false' || github.event_name == 'schedule'
+    if: needs.check_paths.outputs.githubfolder == 'false' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
     steps:
       - name: Checkout
         uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
@@ -70,7 +71,7 @@ jobs:
       - name: Login to Docker Hub
         uses: docker/login-action@b45d80f862d83dbcd57f89517bcf500b2ab88fb2 # v4.0.0
         with:
-          username: teslamate
+          username: ${{ secrets.DOCKER_USERNAME }}
           password: ${{ secrets.DOCKER_PASSWORD }}
 
       - uses: ./.github/actions/merge
@@ -83,7 +84,7 @@ jobs:
             type=edge
   grafana:
     needs: check_paths
-    if: needs.check_paths.outputs.githubfolder == 'false' || github.event_name == 'schedule'
+    if: needs.check_paths.outputs.githubfolder == 'false' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
     runs-on: ubuntu-24.04
     timeout-minutes: 10
     steps:
@@ -93,11 +94,12 @@ jobs:
       - name: Login to Docker Hub
         uses: docker/login-action@b45d80f862d83dbcd57f89517bcf500b2ab88fb2 # v4.0.0
         with:
-          username: teslamate
+          username: ${{ secrets.DOCKER_USERNAME }}
           password: ${{ secrets.DOCKER_PASSWORD }}
 
       - uses: ./.github/actions/grafana
         with:
+          image: ${{ secrets.DOCKER_USERNAME }}/grafana
           tags: |
             type=edge
             type=schedule,pattern=edge
diff --git a/README.md b/README.md
index 36c71c64..23a32161 100644
--- a/README.md
+++ b/README.md
@@ -1,4 +1,4 @@
-# TeslaMate
+# TeslaMate fork
 
 [![License](https://img.shields.io/badge/license-AGPL--3.0-green.svg)](https://github.com/teslamate-org/teslamate/blob/main/LICENSE)
 [![OpenSSF Best Practices](https://www.bestpractices.dev/projects/10859/badge)](https://www.bestpractices.dev/projects/10859)
